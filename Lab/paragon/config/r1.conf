policy-options {    
    policy-statement from_static {
        term 1 {
            from protocol static;
            then accept;
        }
        term default {
            then accept;
        }
    }
}
protocols {
    isis {
        export from_static;
    }
}